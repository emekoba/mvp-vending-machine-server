import { createProductReq, createProductRes } from 'src/dto/product.dto';

export class ProductService {
  async createProduct(payload: createProductReq): Promise<createProductRes> {
    return;
  }
}
